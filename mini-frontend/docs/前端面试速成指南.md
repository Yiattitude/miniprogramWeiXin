# 前端面试速成指南 🚀

> 最后更新：2026年3月11日
> 适用于：快速准备前端面试，重点突击核心知识

---

## 📋 目录

1. [技术栈概览](#技术栈概览)
2. [核心知识点速查](#核心知识点速查)
3. [高频面试题](#高频面试题)
4. [项目经验准备](#项目经验准备)
5. [算法与编程题](#算法与编程题)
6. [面试前检查清单](#面试前检查清单)

---

## 技术栈概览

### 必须掌握的核心技术

```
基础三件套
├── HTML5 (语义化、SEO、可访问性)
├── CSS3 (布局、动画、响应式)
└── JavaScript (ES6+、异步编程、原型链)

框架层
├── Vue 3 (Composition API、响应式原理)
├── React (Hooks、虚拟DOM、状态管理)
└── 小程序 (微信/uni-app)

工程化
├── 构建工具 (Vite、Webpack)
├── 包管理 (npm、pnpm)
├── 版本控制 (Git)
└── 代码规范 (ESLint、Prettier)

进阶技能
├── TypeScript (类型系统)
├── 性能优化 (懒加载、缓存、CDN)
├── 网络协议 (HTTP/HTTPS、WebSocket)
└── 前后端交互 (RESTful API、GraphQL)
```

---

## 核心知识点速查

### 1. JavaScript 核心

#### 数据类型（必考）
```javascript
// 7种基本类型 + 1种引用类型
基本类型: Number, String, Boolean, Undefined, Null, Symbol, BigInt
引用类型: Object (Array, Function, Date, RegExp, Map, Set...)

// 类型判断
typeof 123                  // "number"
typeof null                 // "object" (历史遗留bug)
Array.isArray([])          // true
Object.prototype.toString.call([])  // "[object Array]"
```

#### 闭包（Closure）⭐⭐⭐
```javascript
// 定义：函数能够访问外部作用域的变量
function outer() {
  let count = 0;
  return function inner() {
    count++;
    return count;
  }
}
const counter = outer();
console.log(counter()); // 1
console.log(counter()); // 2

// 应用场景：
// 1. 数据私有化
// 2. 防抖/节流
// 3. 柯里化
// 4. 模块化
```

#### 原型与原型链⭐⭐⭐
```javascript
// 核心概念
function Person(name) {
  this.name = name;
}
Person.prototype.sayHello = function() {
  console.log(`Hello, ${this.name}`);
}

const p = new Person('张三');
// p.__proto__ === Person.prototype
// Person.prototype.__proto__ === Object.prototype
// Object.prototype.__proto__ === null

// 继承方式
// 1. 原型链继承
// 2. 构造函数继承
// 3. 组合继承
// 4. ES6 Class继承（语法糖）
```

#### 异步编程⭐⭐⭐⭐⭐
```javascript
// 1. 回调函数（Callback）
setTimeout(() => {
  console.log('异步任务');
}, 1000);

// 2. Promise
fetch('/api/data')
  .then(res => res.json())
  .then(data => console.log(data))
  .catch(err => console.error(err));

// 3. Async/Await（推荐）
async function fetchData() {
  try {
    const res = await fetch('/api/data');
    const data = await res.json();
    return data;
  } catch (error) {
    console.error(error);
  }
}

// 事件循环（Event Loop）
console.log('1');
setTimeout(() => console.log('2'), 0);
Promise.resolve().then(() => console.log('3'));
console.log('4');
// 输出顺序：1, 4, 3, 2
// 原因：宏任务(setTimeout) vs 微任务(Promise)
```

#### this 指向⭐⭐⭐
```javascript
// 规则：
// 1. 默认绑定：非严格模式指向window，严格模式undefined
// 2. 隐式绑定：谁调用指向谁
// 3. 显式绑定：call/apply/bind
// 4. new 绑定：指向新创建的对象
// 5. 箭头函数：继承外层作用域的this

const obj = {
  name: '对象',
  fn() { console.log(this.name); },
  arrow: () => { console.log(this.name); }
};
obj.fn();     // "对象"
obj.arrow();  // undefined (继承外层)

// 面试常考：手写call/apply/bind
Function.prototype.myCall = function(context, ...args) {
  context = context || window;
  const fn = Symbol();
  context[fn] = this;
  const result = context[fn](...args);
  delete context[fn];
  return result;
};
```

---

### 2. CSS 核心

#### 盒模型
```css
/* 标准盒模型 */
box-sizing: content-box;  /* width = content */

/* IE盒模型（推荐） */
box-sizing: border-box;   /* width = content + padding + border */
```

#### 布局方式⭐⭐⭐⭐⭐
```css
/* 1. Flexbox（一维布局） */
.container {
  display: flex;
  justify-content: center;  /* 主轴对齐 */
  align-items: center;      /* 交叉轴对齐 */
  flex-wrap: wrap;          /* 换行 */
}

/* 2. Grid（二维布局） */
.grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 20px;
}

/* 3. 定位 */
position: static | relative | absolute | fixed | sticky;

/* 4. 浮动（逐渐淘汰） */
float: left | right;
clear: both;
```

#### BFC（块级格式化上下文）⭐⭐⭐
```css
/* 触发BFC的方式 */
overflow: hidden | auto;
display: inline-block | flex | grid;
position: absolute | fixed;
float: left | right;

/* 应用场景 */
/* 1. 清除浮动 */
/* 2. 防止margin重叠 */
/* 3. 自适应两栏布局 */
```

#### 居中方案（必会）
```css
/* 水平居中 */
.center {
  margin: 0 auto;  /* 块级元素 */
  text-align: center;  /* 行内元素 */
}

/* 垂直居中 */
.vertical {
  /* 方法1: Flexbox */
  display: flex;
  align-items: center;
  
  /* 方法2: 绝对定位 + transform */
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  
  /* 方法3: Grid */
  display: grid;
  place-items: center;
}
```

---

### 3. Vue 核心（基于你的项目）

#### 响应式原理⭐⭐⭐⭐
```javascript
// Vue 2: Object.defineProperty
Object.defineProperty(obj, 'key', {
  get() { /* 依赖收集 */ },
  set(newVal) { /* 触发更新 */ }
});

// Vue 3: Proxy（性能更好）
const reactive = new Proxy(target, {
  get(target, key) {
    track(target, key);  // 收集依赖
    return target[key];
  },
  set(target, key, value) {
    target[key] = value;
    trigger(target, key);  // 触发更新
    return true;
  }
});
```

#### 生命周期⭐⭐⭐
```javascript
// Vue 3 Composition API
import { onMounted, onUnmounted } from 'vue';

setup() {
  onMounted(() => {
    // 组件挂载后执行
  });
  
  onUnmounted(() => {
    // 组件卸载前执行（清理定时器、事件监听）
  });
}

// 完整生命周期顺序：
// beforeCreate → created → beforeMount → mounted
// → beforeUpdate → updated → beforeUnmount → unmounted
```

#### 组件通信⭐⭐⭐⭐
```javascript
// 1. Props / Emit（父子组件）
// 父组件
<ChildComponent :data="data" @update="handleUpdate" />

// 子组件
const props = defineProps(['data']);
const emit = defineEmits(['update']);
emit('update', newValue);

// 2. Provide / Inject（跨层级）
// 祖先组件
provide('key', value);
// 后代组件
const value = inject('key');

// 3. Pinia/Vuex（全局状态）
import { useVolunteerStore } from '@/stores/volunteer';
const store = useVolunteerStore();

// 4. EventBus（不推荐）
// 5. $refs（访问子组件实例）
```

#### 虚拟DOM与Diff算法⭐⭐⭐
```javascript
// 虚拟DOM：用JS对象描述DOM结构
const vnode = {
  tag: 'div',
  props: { class: 'container' },
  children: [
    { tag: 'span', children: 'Hello' }
  ]
};

// Diff算法核心思想：
// 1. 同层比较（不跨层级）
// 2. 类型不同直接替换
// 3. 类型相同比较属性和子节点
// 4. key的作用：复用节点，减少DOM操作
```

---

### 4. 工程化

#### Webpack vs Vite⭐⭐⭐
```javascript
// Webpack：打包所有资源
module.exports = {
  entry: './src/main.js',
  output: { filename: 'bundle.js' },
  module: {
    rules: [
      { test: /\.vue$/, loader: 'vue-loader' },
      { test: /\.css$/, use: ['style-loader', 'css-loader'] }
    ]
  },
  plugins: [new HtmlWebpackPlugin()]
};

// Vite：基于ESM，开发时按需编译（快）
export default {
  server: { port: 3000 },
  build: { outDir: 'dist' }
};

// 核心区别：
// Webpack: 打包后启动开发服务器
// Vite: 先启动，请求时编译（速度快10倍+）
```

#### 性能优化⭐⭐⭐⭐⭐
```javascript
// 1. 代码层面
// - 懒加载/按需加载
const ProfilePage = () => import('./pages/profile.vue');

// - 防抖/节流
function debounce(fn, delay) {
  let timer = null;
  return function(...args) {
    clearTimeout(timer);
    timer = setTimeout(() => fn.apply(this, args), delay);
  };
}

// - 虚拟列表（大量数据）
// - 避免不必要的渲染

// 2. 资源优化
// - 图片压缩、WebP格式
// - 雪碧图、iconfont
// - CDN加速
// - Gzip压缩
// - Tree Shaking（移除未使用代码）

// 3. 网络优化
// - HTTP/2多路复用
// - 缓存策略（强缓存、协商缓存）
// - 预加载（preload/prefetch）
// - 减少请求次数

// 4. 首屏优化
// - SSR（服务端渲染）
// - 骨架屏
// - Loading动画
// - 关键CSS内联
```

---

## 高频面试题

### JavaScript篇

#### 1. 深拷贝与浅拷贝⭐⭐⭐⭐
```javascript
// 浅拷贝
const obj1 = { a: 1, b: { c: 2 } };
const shallow = { ...obj1 };  // 或 Object.assign({}, obj1)
shallow.b.c = 3;  // 会影响原对象

// 深拷贝
// 方法1：JSON（无法处理函数、Date、RegExp等）
const deep1 = JSON.parse(JSON.stringify(obj1));

// 方法2：递归实现（完整版）
function deepClone(obj, map = new WeakMap()) {
  if (obj === null || typeof obj !== 'object') return obj;
  if (map.has(obj)) return map.get(obj);  // 处理循环引用
  
  const clone = Array.isArray(obj) ? [] : {};
  map.set(obj, clone);
  
  for (let key in obj) {
    if (obj.hasOwnProperty(key)) {
      clone[key] = deepClone(obj[key], map);
    }
  }
  return clone;
}

// 方法3：structuredClone（现代浏览器）
const deep3 = structuredClone(obj1);
```

#### 2. 数组去重⭐⭐⭐
```javascript
const arr = [1, 2, 2, 3, 4, 4, 5];

// 方法1：Set（推荐）
const unique1 = [...new Set(arr)];

// 方法2：filter
const unique2 = arr.filter((item, index) => arr.indexOf(item) === index);

// 方法3：reduce
const unique3 = arr.reduce((acc, cur) => {
  return acc.includes(cur) ? acc : [...acc, cur];
}, []);
```

#### 3. 手写Promise⭐⭐⭐⭐⭐
```javascript
class MyPromise {
  constructor(executor) {
    this.state = 'pending';
    this.value = undefined;
    this.callbacks = [];
    
    const resolve = (value) => {
      if (this.state !== 'pending') return;
      this.state = 'fulfilled';
      this.value = value;
      this.callbacks.forEach(cb => cb.onFulfilled(value));
    };
    
    const reject = (reason) => {
      if (this.state !== 'pending') return;
      this.state = 'rejected';
      this.value = reason;
      this.callbacks.forEach(cb => cb.onRejected(reason));
    };
    
    try {
      executor(resolve, reject);
    } catch (error) {
      reject(error);
    }
  }
  
  then(onFulfilled, onRejected) {
    return new MyPromise((resolve, reject) => {
      if (this.state === 'fulfilled') {
        setTimeout(() => {
          try {
            const result = onFulfilled(this.value);
            resolve(result);
          } catch (error) {
            reject(error);
          }
        });
      } else if (this.state === 'rejected') {
        setTimeout(() => {
          try {
            const result = onRejected(this.value);
            resolve(result);
          } catch (error) {
            reject(error);
          }
        });
      } else {
        this.callbacks.push({ onFulfilled, onRejected });
      }
    });
  }
}
```

#### 4. 防抖和节流⭐⭐⭐⭐⭐
```javascript
// 防抖：n秒内只执行一次，若重复触发则重新计时
function debounce(fn, delay) {
  let timer = null;
  return function(...args) {
    clearTimeout(timer);
    timer = setTimeout(() => {
      fn.apply(this, args);
    }, delay);
  };
}
// 应用：搜索框输入、窗口resize

// 节流：n秒内只执行一次，周期内重复触发无效
function throttle(fn, delay) {
  let lastTime = 0;
  return function(...args) {
    const now = Date.now();
    if (now - lastTime >= delay) {
      lastTime = now;
      fn.apply(this, args);
    }
  };
}
// 应用：滚动事件、按钮点击
```

---

### CSS篇

#### 1. 实现三栏布局（左右固定，中间自适应）⭐⭐⭐
```css
/* 方法1：Flexbox */
.container {
  display: flex;
}
.left, .right { width: 200px; }
.center { flex: 1; }

/* 方法2：Grid */
.container {
  display: grid;
  grid-template-columns: 200px 1fr 200px;
}

/* 方法3：绝对定位 */
.left { position: absolute; left: 0; width: 200px; }
.right { position: absolute; right: 0; width: 200px; }
.center { margin: 0 200px; }
```

#### 2. 清除浮动⭐⭐⭐
```css
/* 方法1：clearfix（推荐） */
.clearfix::after {
  content: '';
  display: block;
  clear: both;
}

/* 方法2：触发BFC */
.container {
  overflow: hidden;
}
```

---

### Vue篇

#### 1. v-if vs v-show⭐⭐⭐
```vue
<!-- v-if: 条件渲染，真正的增删DOM -->
<div v-if="show">内容</div>

<!-- v-show: display: none，DOM始终存在 -->
<div v-show="show">内容</div>

<!-- 使用场景 -->
<!-- v-if: 切换频率低，初始渲染性能要求高 -->
<!-- v-show: 切换频率高 -->
```

#### 2. computed vs watch⭐⭐⭐
```javascript
// computed: 计算属性，有缓存，依赖变化才重新计算
const fullName = computed(() => {
  return `${firstName.value} ${lastName.value}`;
});

// watch: 侦听器，执行异步或开销大的操作
watch(searchText, async (newVal) => {
  const result = await fetchData(newVal);
  // ...
});
```

#### 3. nextTick原理⭐⭐⭐
```javascript
// 作用：在DOM更新后执行回调
await nextTick();
console.log('DOM已更新');

// 原理：将回调放入微任务队列
// Promise.then → MutationObserver → setImmediate → setTimeout
```

---

## 项目经验准备

### 基于你的志愿活动小程序项目

#### 1. 项目介绍（1分钟版本）
```
这是一个志愿活动管理小程序，主要功能包括：
- 活动发布与报名
- 签到打卡（支持照片上传）
- 个人志愿记录
- 数据统计展示

技术栈：
- 前端：uni-app + Vue 3 + TypeScript + Pinia
- 构建：Vite
- UI：自定义组件库
- 云服务：云函数 + 云数据库

我负责的核心模块：
1. 活动报名与签到流程
2. 图片上传组件封装
3. 状态管理（Pinia）
4. API接口对接

项目亮点：
- 使用TypeScript提升代码可维护性
- 组件化开发提高复用性
- 统一状态管理降低复杂度
```

#### 2. 技术难点与解决方案⭐⭐⭐⭐⭐

##### 难点1：图片上传组件封装
```vue
<!-- 问题：多个页面需要上传图片，重复代码多 -->
<!-- 解决：封装通用UploadImage组件 -->
<template>
  <view class="upload-wrapper">
    <view v-for="(img, index) in images" :key="index">
      <image :src="img" />
      <view @click="removeImage(index)">删除</view>
    </view>
    <view @click="chooseImage" v-if="images.length < max">
      + 添加图片
    </view>
  </view>
</template>

<script setup>
const props = defineProps({
  modelValue: Array,
  max: { type: Number, default: 9 }
});
const emit = defineEmits(['update:modelValue']);

// 关键点：
// 1. v-model双向绑定
// 2. 图片压缩
// 3. 上传进度提示
// 4. 错误处理
</script>
```

##### 难点2：状态管理（Pinia）
```typescript
// stores/volunteer.ts
import { defineStore } from 'pinia';

export const useVolunteerStore = defineStore('volunteer', () => {
  const activities = ref<Activity[]>([]);
  const loading = ref(false);
  
  // 获取活动列表
  async function fetchActivities() {
    loading.value = true;
    try {
      const res = await volunteerApi.getActivities();
      activities.value = res.data;
    } catch (error) {
      console.error(error);
    } finally {
      loading.value = false;
    }
  }
  
  return { activities, loading, fetchActivities };
});

// 优势：
// 1. 类型安全（TypeScript）
// 2. 组合式API风格
// 3. DevTools支持
```

##### 难点3：列表性能优化
```vue
<!-- 问题：活动列表数据量大，滚动卡顿 -->
<!-- 解决方案： -->
<!-- 1. 虚拟列表（长列表） -->
<!-- 2. 分页加载 -->
<!-- 3. 上拉加载更多 -->

<script setup>
const page = ref(1);
const list = ref([]);
const hasMore = ref(true);

async function loadMore() {
  if (!hasMore.value) return;
  
  const res = await fetchList(page.value);
  list.value.push(...res.data);
  page.value++;
  hasMore.value = res.hasMore;
}

onReachBottom(() => {
  loadMore();
});
</script>
```

#### 3. 面试官常问问题

**Q: 为什么选择uni-app？**
A: 跨平台支持（微信/支付宝/H5），一套代码多端运行，降低开发成本。Vue语法熟悉，上手快。

**Q: TypeScript的优势？**
A: 
1. 类型检查，减少runtime错误
2. 智能提示，提高开发效率
3. 代码可维护性更好
4. 重构更安全

**Q: 如何保证代码质量？**
A:
1. ESLint + Prettier 统一代码风格
2. TypeScript 类型检查
3. 组件化开发，单一职责
4. 封装通用工具函数
5. 错误边界处理

**Q: 遇到的最大挑战？**
A: （结合实际情况）例如：小程序平台限制、云函数调试困难、多端兼容性问题等，以及如何解决。

---

## 算法与编程题

### 高频算法题（LeetCode Top 20）

#### 1. 两数之和⭐⭐⭐
```javascript
function twoSum(nums, target) {
  const map = new Map();
  for (let i = 0; i < nums.length; i++) {
    const complement = target - nums[i];
    if (map.has(complement)) {
      return [map.get(complement), i];
    }
    map.set(nums[i], i);
  }
  return [];
}
// 时间复杂度：O(n)
```

#### 2. 反转链表⭐⭐⭐⭐
```javascript
function reverseList(head) {
  let prev = null;
  let curr = head;
  while (curr) {
    const next = curr.next;
    curr.next = prev;
    prev = curr;
    curr = next;
  }
  return prev;
}
```

#### 3. 最大子数组和（动态规划）⭐⭐⭐
```javascript
function maxSubArray(nums) {
  let max = nums[0];
  let sum = nums[0];
  for (let i = 1; i < nums.length; i++) {
    sum = Math.max(nums[i], sum + nums[i]);
    max = Math.max(max, sum);
  }
  return max;
}
```

#### 4. 快速排序⭐⭐⭐⭐
```javascript
function quickSort(arr) {
  if (arr.length <= 1) return arr;
  const pivot = arr[Math.floor(arr.length / 2)];
  const left = arr.filter(x => x < pivot);
  const middle = arr.filter(x => x === pivot);
  const right = arr.filter(x => x > pivot);
  return [...quickSort(left), ...middle, ...quickSort(right)];
}
```

#### 5. 二分查找⭐⭐⭐
```javascript
function binarySearch(arr, target) {
  let left = 0, right = arr.length - 1;
  while (left <= right) {
    const mid = Math.floor((left + right) / 2);
    if (arr[mid] === target) return mid;
    if (arr[mid] < target) left = mid + 1;
    else right = mid - 1;
  }
  return -1;
}
```

---

## 面试前检查清单

### 📝 知识准备（3-7天）

- [ ] **JavaScript基础**
  - [ ] 数据类型、类型转换
  - [ ] 闭包、作用域链
  - [ ] 原型与原型链
  - [ ] 异步编程（Promise、async/await）
  - [ ] ES6+新特性

- [ ] **CSS基础**
  - [ ] 盒模型、BFC
  - [ ] Flex、Grid布局
  - [ ] 居中方案
  - [ ] 响应式设计

- [ ] **Vue/React框架**
  - [ ] 生命周期
  - [ ] 组件通信
  - [ ] 状态管理
  - [ ] 响应式原理
  - [ ] 虚拟DOM

- [ ] **工程化**
  - [ ] Webpack/Vite配置
  - [ ] 性能优化
  - [ ] 代码规范

- [ ] **网络协议**
  - [ ] HTTP/HTTPS
  - [ ] 跨域解决方案
  - [ ] 浏览器缓存

- [ ] **算法**
  - [ ] 数组、链表、树
  - [ ] 排序、查找
  - [ ] 动态规划（可选）

### 💼 项目准备（1-2天）

- [ ] 梳理项目架构
- [ ] 准备技术难点
- [ ] 总结项目亮点
- [ ] 准备3个以上项目问题及解决方案
- [ ] 准备项目Demo演示（可选）

### 🎯 模拟面试（面试前1天）

- [ ] 自我介绍（1分钟）
- [ ] 项目介绍（3分钟）
- [ ] 刷10道高频题
- [ ] 准备3个问题问面试官

### 🔧 其他准备

- [ ] GitHub代码整理（Profile、README）
- [ ] 简历更新（突出技术栈、项目经验）
- [ ] 准备笔记本、笔（线下面试）
- [ ] 测试网络、摄像头（线上面试）
- [ ] 充足睡眠

---

## 💡 速成技巧

### 1. 时间分配（假设7天准备期）

| 天数 | 重点内容 | 时长 |
|-----|---------|------|
| Day 1-2 | JavaScript核心（闭包、原型、异步） | 6h/天 |
| Day 3-4 | Vue/React + 项目复盘 | 6h/天 |
| Day 5 | CSS + 性能优化 | 6h |
| Day 6 | 算法 + 手写代码 | 6h |
| Day 7 | 模拟面试 + 查漏补缺 | 4h |

### 2. 学习策略

1. **理解 > 记忆**：理解原理，不要死记硬背
2. **实践 > 理论**：动手写代码，加深理解
3. **高频 > 全面**：优先掌握80%的高频题
4. **总结 > 刷题**：总结规律和套路

### 3. 面试技巧

- **STAR法则**介绍项目：
  - Situation（背景）
  - Task（任务）
  - Action（行动）
  - Result（结果）

- **不会的问题**：
  - 诚实说不会，但表达学习意愿
  - 说出相关知识点，展示思考能力

- **追问时**：
  - 深挖技术细节
  - 说出多种解决方案并对比

---

## 📚 推荐资源

### 学习网站
- [MDN Web Docs](https://developer.mozilla.org/) - 最权威的前端文档
- [Vue.js官方文档](https://cn.vuejs.org/) - Vue 3中文文档
- [LeetCode](https://leetcode.cn/) - 算法刷题

### 面试题库
- 牛客网前端面试题
- 掘金面试专栏
- GitHub: `qianguyihao/Web`、`haizlin/fe-interview`

### 博客推荐
- 阮一峰的网络日志
- 神三元的博客
- 冴羽的博客

---

## ⚡ 最后的话

> "面试不是终点，而是新的起点。"

### 心态调整
1. **自信但不自负**：准备充分，相信自己
2. **紧张是正常的**：深呼吸，慢慢说
3. **失败是学习机会**：每次面试都是成长

### 面试当天
- 提前15分钟到达（线下）/ 登录（线上）
- 礼貌问候，保持微笑
- 思考清楚再回答，不要抢答
- 感谢面试官的时间

---

### 🎯 速记卡片（考前30分钟看）

```
核心记忆点：
1. 闭包 = 函数 + 外部变量引用
2. 原型链 = __proto__ 链条，一直到 Object.prototype
3. Event Loop = 宏任务(setTimeout) 后执行，微任务(Promise) 先执行
4. this = 谁调用指向谁，箭头函数除外
5. 响应式 = Vue 2 (Object.defineProperty) vs Vue 3 (Proxy)
6. 虚拟DOM = JS对象描述DOM，Diff算法对比差异
7. Flexbox = 一维布局，justify-content + align-items
8. 性能优化 = 懒加载、防抖节流、缓存、代码分割
9. 跨域 = CORS、代理、JSONP
10. 项目亮点 = 技术栈 + 解决方案 + 优化成果
```

---

**祝你面试顺利！加油！💪**

如有疑问，随时查阅本文档。记住：准备充分 = 信心满满！
