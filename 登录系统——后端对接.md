
登录页和“姓名+手机号”表单页都已就绪，且登录成功后会根据 needBinding 或缺少资料跳转到表单页。mini-frontend/src/pages/auth/login.vue:71 mini-frontend/src/pages/auth/login.vue:78
目前云函数里没有实现 wechatLogin/bindUser，前端一调用就会报“未定义业务动作”，所以登录链路会断在后端。mini-frontend/cloudfunctions/volunteer-service/index.js:15
如果后端不返回 userInfo（包含 realName/phone），前端会默认认为“资料不完整”而强制去表单页。mini-frontend/src/pages/auth/login.vue:46
建议

后端实现 wechatLogin 和 bindUser，并在已绑定时返回 userInfo（含 realName/phone）或明确 needBinding=false。
若后端暂时不能返回 userInfo，我可以把前端逻辑改成“只看 needBinding”，避免所有用户都被强制去表单。